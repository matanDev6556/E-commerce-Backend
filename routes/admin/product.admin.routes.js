const express = require('express');
const router = express.Router();


/*
module.exports = (productAdminController) => {
  // products
  router.get('/count', productAdminController.getProductsCount);
  router.post('/', productAdminController.addProduct);
  router.put('/:id', productAdminController.editProduct);
  router.delete('/:id/', productAdminController.deleteProduct);
  router.delete(
    '/:id/images',
    productAdminController.deleteProductImages
  );
  return router;
};
*/
